# ============================================================
# Shared — Python / env / editor / OS
# ============================================================
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.env
*.env
.env.*
.vscode/
.cursor/
.idea/
.DS_Store

# ============================================================
# Downstream literature RAG (momo half) — local outputs & logs.
# NOTE: data/ is intentionally TRACKED on main for team handoff (public literature).
# ============================================================
outputs/
outputs_baseline_v1/
logs/
_nohup_embed.out
_embed_multimodal_*.log
_mm_regen.log
orchestrator_tools/*.out
orchestrator_tools/*.err
orchestrator_tools/.last_all_jobid
orchestrator_runs
README_KR.md

# ============================================================
# Agentic workflow (RAG Agent/) — MIMIC-IV / PHI. NEVER commit.
# Credentialed PhysioNet data, patient-derived stores, run transcripts.
# Format rules are scoped to "RAG Agent/" so momo's literature data/ is unaffected.
# ============================================================
ckd_samples/
lookup_tables/
patient_db/
NotesDB/
runs/
RAG Agent/**/cohort_nodes.csv
RAG Agent/**/*_chunks.jsonl
RAG Agent/sql_agent/metadata.yaml

# Patient-derived data formats — scoped to the agentic workflow only
RAG Agent/**/*.csv
RAG Agent/**/*.parquet
RAG Agent/**/*.pkl
RAG Agent/**/*.npy
RAG Agent/**/*.yaml
RAG Agent/**/*.yml
RAG Agent/**/*.ipynb

# ============================================================
# Misc logs / backups
# ============================================================
*.log
*.err
*.out
*.bak
